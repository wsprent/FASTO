fun bool main() =
  let x0 = write(true  && false) in
  let x1 = write(false && true) in
  let x2 = write(true  || false) in
  let x3 = write(false || true) in
  let x4 = write(x0 || x2 && x3 || x0) in
  write(x4 || true)
